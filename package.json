{
  "name": "medtrack",
  "version": "1.0.0",
  "description": "Blockchain-secured medical records DApp on Sepolia",
  "scripts": {
    "install:all": "npm install && cd backend && npm install && cd ../client && npm install && cd ../smart_contracts && npm install",
    "dev:backend": "cd backend && npm run dev",
    "dev:client": "cd client && npm run dev",
    "deploy:contract": "cd smart_contracts && npm run deploy:sepolia"
  },
  "keywords": ["blockchain", "healthcare", "solidity", "nextjs", "ipfs"],
  "license": "MIT"
}
